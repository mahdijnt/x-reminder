# Dashboard Deployment Guide

## Runtime
- Node.js 20+
- Next.js (`next start`)

## Steps
1. Build `dashboard/` with `npm run build`.
2. Set `NEXT_PUBLIC_API_BASE_URL` and `NEXT_PUBLIC_APP_URL`.
3. Start with `npm run start`.
