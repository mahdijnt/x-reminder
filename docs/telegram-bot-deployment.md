# Telegram Bot Deployment Guide

## Runtime
- Python 3.11
- `TELEGRAM_BOT_TOKEN`

## Steps
1. Build with `telegram-bot/Dockerfile` or install dependencies.
2. Set token and backend URL env vars.
3. Run `python main.py`.
